load("@rules_python//python:defs.bzl", "py_library")
load("//bazel:lambda.bzl", "lambda_zip")

py_library(
    name = "lib",
    srcs = glob(["*.py"]),
    deps = ["//src/doubleday:_pkg"],
)

lambda_zip(
    name = "check_failures",
    deps = [":lib"],
)
